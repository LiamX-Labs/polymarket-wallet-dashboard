PORT=3001

# Path to the EXISTING tracker database (READ-ONLY)
# This is the database created by market_tracker.py - we only READ from it
TRACKER_DB_PATH=/home/william/STRATEGIES/Polymarkets/data/tracker.db

# Path to the NEW dashboard database (SEPARATE - will be created automatically)
# This is a completely separate database just for the dashboard
DASHBOARD_DB_PATH=/home/william/polymarket-wallet-dashboard/data/dashboard.db
